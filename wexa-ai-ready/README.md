# Wexa AI — Ready-to-Deploy Scaffold (Railway + Vercel)
See instructions inside for Railway (API) and Vercel (client). Set `VITE_API_BASE` on Vercel, and `MONGO_URI`, `JWT_SECRET`, `CORS_ORIGIN` on Railway.
